import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container : {
        flex: 1,
        backgroundColor: '#000',
    },

    teacherList : {
        marginTop: -40,
    },
});

export default styles;